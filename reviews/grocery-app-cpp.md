# Grocery App (C++)

Console app that tracked grocery item purchase frequency. `std::map<std::string, int>` or similar. The repo is gone now (404, no Wayback snapshot).

From memory: it worked, it counted things, it sorted output. Standard intro C++ assignment. Almost certainly had at least one `cin >>` / `getline()` buffer mismatch because every SNHU C++ project from that era did.

**Archive.** Can't even show it anymore. Standard map exercise, nothing notable.
